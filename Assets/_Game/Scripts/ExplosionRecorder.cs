using System.Collections;
using System.Collections.Generic;
//using UnityEditor.Animations;
using UnityEngine;

public class ExplosionRecorder : MonoBehaviour
{
    /*public AnimationClip clip;

    public bool record = false;

    private GameObjectRecorder recorder;

    private void Start()
    {
        recorder = new(gameObject);
        recorder.BindComponentsOfType<Transform>(gameObject, true);
        
    }

    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.H)) record = !record;
        if (Input.GetKeyDown(KeyCode.J)) record = !record;
        if (record)
        {
            recorder.TakeSnapshot(Time.deltaTime);
        }
        else if (recorder.isRecording)
        {
            recorder.SaveToClip(clip);
            recorder.ResetRecording();
        }
    }*/
}
